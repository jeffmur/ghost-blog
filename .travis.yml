language: ruby
cache: bundler
rvm:
- 2.7.0
install:
- bundle install
script: bundle exec rake site:deploy --quiet --trace
branches:
  only:
  - main
git: 
  depth: false
deploy:
  provider: pages
  github_token: $GITHUB_TOKEN
  on:
    branch: gh-pages
  target_branch: main
  fqdn: jeffmur.dev
